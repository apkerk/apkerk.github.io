# manuscripts

Drop hosted PDFs here.

- `jmp.pdf` — the full job market paper, "Approval Without Amplification." When added, set `jmpManuscript.ready = true` and `url` in `/assets/js/data.js`, and replace the "Email me for the current draft" button on the JMP page with a read/download link.

Do NOT place the Separate Spheres draft here. It is embargoed until Organization Science publishes it.
